@model IEnumerable<WebApplication1.Models.EvenementModel>
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/Admin.cshtml";
}
                        <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">

                            
                            <thead>
                                <tr>
                                    <th>Title</th>
                                    <th>Start_Date</th>
                                    <th>Finish_Date</th>
                                    <th>Description</th>
                                   
                                   
                                   
                                    <th>Actions</th>
                                   
                                </tr>

                            <tbody>
                                @foreach (var p in Model)
                                {
                                    <tr>
                                        <td>@p.Title</td>
                                        <td>@Convert.ToString(string.Format("{0:dd/MM/yyyy}", p.Start_Date))</td>
                                        <td>@Convert.ToString(string.Format("{0:dd/MM/yyyy}", p.Finish_Date))</td>
                                        <td>@p.Description</td>
                                       

                                     
                                      
                                            <td>@Html.ActionLink("Edit", "Edit", new { id = @p.EvenementId }) |
                                         @Html.ActionLink("Details", "Details", new { id = @p.EvenementId }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = @p.EvenementId })</td>

                                    </tr>
                                }
                            </tbody>

                        </table>
                       
                    </div>

                </div>
                <a href="@Url.Action("ExportPdf", "Evenement")">Download PDF</a>
                </script>
                
            </div>
        </div>
        <!-- /.container-fluid-->
        <!-- /.content-wrapper-->
   
       